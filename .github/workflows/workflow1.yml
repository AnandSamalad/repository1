name: Animal Farm NodeJS CI
on:
  push:
    branches:
      - "*"
jobs:
  # build1:
  #   runs-on: ubuntu-latest
  #   environment: demo
  #   outputs:
  #     matrix: ${{ steps.demo.outputs.sample }}
  #   steps:
  #   - name: Run tests
  #     run: echo "${{ vars.DEMO }}"
  #   - name: demo
  #     id: demo
  #     run: echo "sample=${{ vars.DEMO }}" >> $GITHUB_OUTPUT

  build2:
    runs-on: ubuntu-latest
    # needs: build1
    environment: 
      name: demo2
    outputs:
      matrix: ${{ steps.demo2.outputs.sample }}
    steps:
    - name: Run tests
      run: echo "${{ vars.DEMO }}"
    - name: demo2
      id: demo2
      run: echo "sample=${{ vars.DEMO2 }}" >> $GITHUB_OUTPUT

  example_matrix:
    runs-on: ${{ matrix.version }}
    needs: build2
    # environment: demo
    strategy:
      matrix:
        version: ${{fromJSON(needs.build2.outputs.matrix)}}
    steps:
    - name: devil
      run: echo "sample"