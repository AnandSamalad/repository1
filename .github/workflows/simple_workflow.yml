name: Simple Workflow

on:
  push:
    branches: [ "*" ]

jobs:
  build:
    runs-on: ${{ vars.runner }}

    steps:
    - name: Step 1
      run: echo "This is step 1"

    - name: Step 2
      run: echo "This is step 2"

